# Only needed when tunneling the backend separately from the frontend
# (e.g. testing via cloudflared/ngrok on a different device).
# Leave unset for normal local development -- it defaults to ws://localhost:8765/ws/session.
VITE_WS_URL=wss://your-backend-tunnel-url/ws/session
